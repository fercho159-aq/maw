import WebsitePortfolioSection from "@/components/sections/website-portfolio";

/**
 * Slot para la landing de "sitio-web": reutiliza la sección editorial de
 * portafolio de sitios (filtros en mono, EditorialImage, grilla escalonada,
 * sin autoplay ni pills).
 */
export default function WebsitesPortfolioSlot() {
  return <WebsitePortfolioSection />;
}
